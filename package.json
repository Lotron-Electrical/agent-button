{
  "name": "agent-button",
  "version": "1.0.0",
  "private": true,
  "description": "Phone button -> relay -> PC poller that spawns Claude Code agent tabs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
